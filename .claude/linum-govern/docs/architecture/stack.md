# Stack — linum-core-portfolio

> Detected by `/init-linum`. Drives which `lg-*` specialists are relevant and which
> validation commands run.

| Aspect | Value |
|--------|-------|
| Package manager | bun |
| Language(s) | TypeScript 5 |
| Framework(s) | Next.js 16.2.6, React 19, Tailwind CSS 4 |
| Test framework | Playwright |
| Lint / format | ESLint 9 (config-next), Tailwind CSS linter |
| Type checking | TypeScript (strict mode) |
| Build | next build |

## Validation commands (also in `config.json`)

```bash
bun run build         # Next.js build
bun run lint          # ESLint check
bun run test:e2e      # Playwright E2E tests
bun run dev           # Development server
```
